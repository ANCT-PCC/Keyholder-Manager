uid,submit_time,receipt_id,front,back,is_completed,is_delivered
登録通し番号,注文時間,伝票番号,表,裏,完成したか,引き渡しをしたか

伝票番号は、「作り直し」になった場合には「伝票番号R」の形式で設定の上、システム上では再注文の扱いをする。
その際、修正前のis_deliveredはtrueとして扱う。
通し番号は、システム上での管理番号であり、伝票番号とは異なる。
→作成状況画面の表示順番に影響する。

コンソールに表示する項目
伝票番号、表の番号、裏の番号、操作ボタン（完成した、絵柄を確認した、やり直す）